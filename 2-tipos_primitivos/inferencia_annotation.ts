/*

INFERENCIA VS ANOTAÇÃO

type annotation: quando o tipo é definido explicitamente
type inference: quando o TS identifica o tipo

*/

const annotation: string = "tipo de dado por annotation";

let inference = "tipo inference";
// ERRO: inference = 10;
